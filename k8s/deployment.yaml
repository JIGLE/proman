apiVersion: apps/v1
kind: Deployment
metadata:
  name: situs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: situs
  template:
    metadata:
      labels:
        app: situs
    spec:
      securityContext:
        fsGroup: 1001
      containers:
        - name: situs
          image: ghcr.io/jigle/situs:<version> # use a specific tag (avoid latest)
          imagePullPolicy: IfNotPresent
          env:
            - name: NODE_ENV
              value: "production"
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: PORT
              value: "3000"
            - name: NEXTAUTH_URL
              value: "https://your.domain"
            - name: NEXTAUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: situs-secrets
                  key: NEXTAUTH_SECRET
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: situs-secrets
                  key: DATABASE_URL
            - name: SENDGRID_API_KEY
              valueFrom:
                secretKeyRef:
                  name: situs-secrets
                  key: SENDGRID_API_KEY
            - name: INIT_SECRET
              valueFrom:
                secretKeyRef:
                  name: situs-secrets
                  key: INIT_SECRET
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/ready
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /api/ready
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: situs-data
              mountPath: /app/data
      volumes:
        - name: situs-data
          persistentVolumeClaim:
            claimName: situs-data
