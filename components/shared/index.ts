/**
 * Shared Components Module
 * 
 * Exports commonly used shared components across the application.
 * Usage: import { ClientProviders, ErrorBoundary, VersionBadge } from '@/shared'
 */

export { ClientProviders } from './client-providers'
export { ErrorBoundary } from './error-boundary'
export { default as VersionBadge } from './version-badge'
